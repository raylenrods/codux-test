import { Line } from 'd3-shape';
import { BaseDatum, ComputedDatum, Variable, TooltipComponent, IfGrouped, ComputedGroupDatum } from '../types';
export declare const ParallelCoordinatesLine: <Datum extends BaseDatum, GroupBy extends Exclude<{ [K in keyof Datum]: Datum[K] extends string ? K : never; }[keyof Datum], "id"> | undefined>({ datum, variables, lineGenerator, lineWidth, opacity, tooltip, testIdPrefix, }: {
    datum: IfGrouped<Datum, GroupBy, ComputedGroupDatum<Datum>, ComputedDatum<Datum>>;
    variables: readonly Variable<Datum>[];
    lineGenerator: Line<[number, number]>;
    lineWidth: number;
    opacity: number;
    tooltip: TooltipComponent<Datum, GroupBy>;
    testIdPrefix?: string | undefined;
}) => import("react/jsx-runtime").JSX.Element;
//# sourceMappingURL=ParallelCoordinatesLine.d.ts.map