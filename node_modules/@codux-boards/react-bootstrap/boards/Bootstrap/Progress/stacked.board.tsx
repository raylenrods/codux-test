import { createBoard } from '@wixc3/react-board';
import ProgressBar from 'react-bootstrap/ProgressBar';
// board cover
import cover from '../../../../assets/progress/stacked.svg';

export default createBoard({
    name: 'Stacked',
    Board: () => {
        return (
            <ProgressBar>
                <ProgressBar striped variant="success" now={44} key={1} />
                <ProgressBar variant="warning" now={22} key={2} />
                <ProgressBar striped variant="primary" now={14} key={3} />
            </ProgressBar>
        );
    },
    tags: ['progressbar', 'stacked', 'primary', 'success', 'warning'],
    cover,
    environmentProps: {
        canvasWidth: 252,
    },
});
