import { createBoard } from '@wixc3/react-board';
import ProgressBar from 'react-bootstrap/ProgressBar';
// board cover
import cover from '../../../../assets/progress/primary-with-label.svg';

export default createBoard({
    name: 'Primary with label',
    Board: () => {
        const now = 60;
        return <ProgressBar variant="primary" now={now} label={`${now}%`} />;
    },
    tags: ['progressbar', 'primary', 'label'],
    cover,
    environmentProps: {
        canvasWidth: 252,
    },
});
