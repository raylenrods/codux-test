import { createBoard } from '@wixc3/react-board';
import ProgressBar from 'react-bootstrap/ProgressBar';
// board cover
import cover from '../../../../assets/progress/danger.svg';

export default createBoard({
    name: 'Danger',
    Board: () => {
        return <ProgressBar variant="danger" now={54} />;
    },
    tags: ['progressbar', 'danger'],
    cover,
    environmentProps: {
        canvasWidth: 252,
    },
});
