import { createBoard } from '@wixc3/react-board';
import ProgressBar from 'react-bootstrap/ProgressBar';
// board cover
import cover from '../../../../assets/progress/primary-striped.svg';

export default createBoard({
    name: 'Primary striped',
    Board: () => {
        return <ProgressBar striped variant="primary" now={80} />;
    },
    tags: ['progressbar', 'primary', 'striped'],
    cover,
    environmentProps: {
        canvasWidth: 252,
    },
});
