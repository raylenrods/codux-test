import { createBoard } from '@wixc3/react-board';
import ProgressBar from 'react-bootstrap/ProgressBar';
// board cover
import cover from '../../../../assets/progress/warning.svg';

export default createBoard({
    name: 'Warning',
    Board: () => {
        return <ProgressBar variant="warning" now={74} />;
    },
    tags: ['progressbar', 'warning'],
    cover,
    environmentProps: {
        canvasWidth: 252,
    },
});
