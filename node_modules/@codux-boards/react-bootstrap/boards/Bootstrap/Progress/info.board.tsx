import { createBoard } from '@wixc3/react-board';
import ProgressBar from 'react-bootstrap/ProgressBar';
// board cover
import cover from '../../../../assets/progress/info.svg';

export default createBoard({
    name: 'Info',
    Board: () => {
        return <ProgressBar variant="info" now={86} />;
    },
    tags: ['progressbar', 'info'],
    cover,
    environmentProps: {
        canvasWidth: 252,
    },
});
