import { createBoard } from '@wixc3/react-board';
import ProgressBar from 'react-bootstrap/ProgressBar';
// board cover
import cover from '../../../../assets/progress/primary.svg';

export default createBoard({
    name: 'Primary',
    Board: () => {
        return <ProgressBar variant="primary" now={44} />;
    },
    tags: ['progressbar', 'primary'],
    cover,
    environmentProps: {
        canvasWidth: 252,
    },
});
