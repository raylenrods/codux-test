import { createBoard } from '@wixc3/react-board';
import ProgressBar from 'react-bootstrap/ProgressBar';
// board cover
import cover from '../../../../assets/progress/success.svg';

export default createBoard({
    name: 'Success',
    Board: () => {
        return <ProgressBar variant="success" now={78} />;
    },
    tags: ['progressbar', 'success'],
    cover,
    environmentProps: {
        canvasWidth: 252,
    },
});
