import { createBoard } from '@wixc3/react-board';
import Spinner from 'react-bootstrap/Spinner';
// board cover
import cover from '../../../../assets/spinners/secondary.svg';

export default createBoard({
    name: 'Secondary',
    Board: () => {
        return <Spinner variant="secondary" animation="border" />;
    },
    tags: ['spinner', 'secondary'],
    cover,
});
