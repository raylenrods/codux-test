import { createBoard } from '@wixc3/react-board';
import Spinner from 'react-bootstrap/Spinner';
// board cover
import cover from '../../../../assets/spinners/info.svg';

export default createBoard({
    name: 'Info',
    Board: () => {
        return <Spinner variant="info" animation="border" />;
    },
    tags: ['spinner', 'info'],
    cover,
});
