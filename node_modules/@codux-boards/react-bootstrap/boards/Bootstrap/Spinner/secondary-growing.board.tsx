import { createBoard } from '@wixc3/react-board';
import Spinner from 'react-bootstrap/Spinner';
// board cover
import cover from '../../../../assets/spinners/secondary-growing.svg';

export default createBoard({
    name: 'Secondary growing',
    Board: () => {
        return <Spinner variant="secondary" animation="grow" />;
    },
    tags: ['spinner', 'secondary', 'grow'],
    cover,
});
