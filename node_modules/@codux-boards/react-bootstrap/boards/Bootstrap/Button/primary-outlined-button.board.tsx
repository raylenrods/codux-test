import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
// board cover
import cover from './primary-outlined-button.svg';

export default createBoard({
    name: 'Primary Outlined Button',
    Board: () => <Button variant="outline-primary">Primary</Button>,
    tags: ['button', 'primary', 'outline'],
    cover,
});
