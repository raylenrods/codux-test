import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';

// board cover
import cover from '../../../../assets/buttons/success.svg';

export default createBoard({
    name: 'Success',
    Board: () => <Button variant="success">Success</Button>,
    tags: ['button', 'success'],
    cover,
});
