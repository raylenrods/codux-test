import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
// board cover
import cover from './danger-button.svg';

export default createBoard({
    name: 'Danger Button',
    Board: () => <Button variant="danger">Danger</Button>,
    tags: ['button', 'danger'],
    cover,
});
