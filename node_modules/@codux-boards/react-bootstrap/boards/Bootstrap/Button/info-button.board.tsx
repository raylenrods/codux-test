import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
// board cover
import cover from './info-button.svg';

export default createBoard({
    name: 'Info Button',
    Board: () => <Button variant="info">Info</Button>,
    tags: ['button', 'info'],
    cover,
});
