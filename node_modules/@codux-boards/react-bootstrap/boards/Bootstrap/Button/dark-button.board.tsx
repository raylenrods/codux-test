import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
// board cover
import cover from './dark-button.svg';

export default createBoard({
    name: 'Dark Button',
    Board: () => <Button variant="dark">Dark</Button>,
    tags: ['button', 'dark'],
    cover,
});
