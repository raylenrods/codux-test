import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
// board cover
import cover from './light-button.svg';

export default createBoard({
    name: 'Light Button',
    Board: () => <Button variant="light">Light</Button>,
    tags: ['button', 'light'],
    cover,
});
