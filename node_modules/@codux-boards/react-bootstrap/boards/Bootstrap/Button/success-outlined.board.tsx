import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';

// board cover
import cover from '../../../../assets/buttons/success-outlined.svg';

export default createBoard({
    name: 'Success outlined',
    Board: () => <Button variant="outline-success">Success</Button>,
    tags: ['button', 'success', 'outline'],
    cover,
});
