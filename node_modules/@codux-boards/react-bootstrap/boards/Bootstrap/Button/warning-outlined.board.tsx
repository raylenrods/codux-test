import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';

// board cover
import cover from '../../../../assets/buttons/warning-outlined.svg';

export default createBoard({
    name: 'Warning outlined',
    Board: () => <Button variant="outline-warning">Warning</Button>,
    tags: ['button', 'warning', 'outline'],
    cover,
});
