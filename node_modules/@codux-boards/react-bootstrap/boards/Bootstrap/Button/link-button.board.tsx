import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
// board cover
import cover from './link-button.svg';

export default createBoard({
    name: 'Link Button',
    Board: () => <Button variant="link">Link</Button>,
    tags: ['button', 'link'],
    cover,
});
