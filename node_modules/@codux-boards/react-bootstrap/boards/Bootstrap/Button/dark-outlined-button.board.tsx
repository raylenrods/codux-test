import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
// board cover
import cover from './dark-outlined-button.svg';

export default createBoard({
    name: 'Dark Outlined Button',
    Board: () => <Button variant="outline-dark">Dark</Button>,
    tags: ['button', 'dark', 'outline'],
    cover,
});
