import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
// board cover
import cover from './danger-outlined-button.svg';

export default createBoard({
    name: 'Danger Outlined Button',
    Board: () => <Button variant="outline-danger">Danger</Button>,
    tags: ['danger', 'outline', 'button'],
    cover,
});
