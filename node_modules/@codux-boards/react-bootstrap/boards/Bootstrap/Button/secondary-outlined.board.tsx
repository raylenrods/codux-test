import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
// board cover
import cover from '../../../../assets/buttons/secondary-outlined.svg';

export default createBoard({
    name: 'Secondary outlined',
    Board: () => <Button variant="outline-secondary">Secondary</Button>,
    tags: ['button', 'secondary', 'outline'],
    cover,
});
