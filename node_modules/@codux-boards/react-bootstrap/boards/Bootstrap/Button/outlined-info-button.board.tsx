import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
// board cover
import cover from './outlined-info-button.svg';

export default createBoard({
    name: 'Outlined Info Button',
    Board: () => <Button variant="outline-info">Info</Button>,
    tags: ['button', 'info', 'outline'],
    cover,
});
