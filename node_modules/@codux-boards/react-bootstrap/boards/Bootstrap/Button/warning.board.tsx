import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';

// board cover
import cover from '../../../../assets/buttons/warning.svg';

export default createBoard({
    name: 'Warning',
    Board: () => <Button variant="warning">Warning</Button>,
    tags: ['button', 'warning'],
    cover,
});
