import { createBoard } from '@wixc3/react-board';
import Alert from 'react-bootstrap/Alert';
//cover
import cover from './info-alert.svg';

export default createBoard({
    name: 'Info Alert',
    Board: () => (
        <Alert variant={'info'}>
            This is a simple info alert - check it out!
        </Alert>
    ),
    tags: ['alert', 'info'],
    cover,
});
