import { createBoard } from '@wixc3/react-board';
import Alert from 'react-bootstrap/Alert';
// board cover
import cover from './dark-alert.svg';

export default createBoard({
    name: 'Dark Alert',
    Board: () => (
        <Alert variant={'dark'}>
            This is a simple dark alert - check it out!
        </Alert>
    ),
    tags: ['alert', 'dark'],
    cover,
});
