import { createBoard } from '@wixc3/react-board';
import Alert from 'react-bootstrap/Alert';
// board cover
import cover from './danger-alert.svg';

export default createBoard({
    name: 'Danger Alert',
    Board: () => (
        <Alert variant={'danger'}>
            This is a simple danger alert - check it out!
        </Alert>
    ),
    tags: ['alert', 'danger'],
    cover,
});
