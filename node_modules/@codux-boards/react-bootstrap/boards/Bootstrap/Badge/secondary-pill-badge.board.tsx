import { createBoard } from '@wixc3/react-board';
import Badge from 'react-bootstrap/Badge';

// board cover
import cover from './secondary-pill-badge.svg';

export default createBoard({
    name: 'Secondary pill badge',
    Board: () => {
        return (
            <Badge pill bg="secondary">
                Secondary
            </Badge>
        );
    },
    tags: ['primary', 'pill', 'badge'],
    cover,
});
