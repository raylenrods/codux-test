import { createBoard } from '@wixc3/react-board';
import Badge from 'react-bootstrap/Badge';

// board cover
import cover from './info-pill-badge.svg';

export default createBoard({
    name: 'Info Pill Badge',
    Board: () => {
        return (
            <Badge pill bg="info">
                Info
            </Badge>
        );
    },
    tags: ['badge', 'pill', 'info'],
    cover,
});
