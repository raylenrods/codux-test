import { createBoard } from '@wixc3/react-board';
import Badge from 'react-bootstrap/Badge';

// board cover
import cover from './dark-pill-badge.svg';

export default createBoard({
    name: 'Dark Pill Badge',
    Board: () => {
        return (
            <Badge pill bg="dark">
                Dark
            </Badge>
        );
    },
    tags: ['badge', 'pill', 'dark'],
    cover,
});
