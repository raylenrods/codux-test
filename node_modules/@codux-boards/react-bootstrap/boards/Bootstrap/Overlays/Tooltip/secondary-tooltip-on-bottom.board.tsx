import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
import OverlayTrigger from 'react-bootstrap/OverlayTrigger';
import Tooltip from 'react-bootstrap/Tooltip';

// board cover
import cover from '../../../../../assets/tooltips/secondary-tooltip-on-bottom.svg';

export default createBoard({
    name: 'Secondary tooltip on bottom',

    Board: () => {
        return (
            <OverlayTrigger
                placement="bottom"
                overlay={
                    <Tooltip id="tooltip-bottom">Tooltip on bottom.</Tooltip>
                }
            >
                <Button variant="secondary">Tooltip on bottom</Button>
            </OverlayTrigger>
        );
    },
    tags: ['tooltip', 'overlay', 'bottom', 'secondary'],
    cover,
});
