import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
import OverlayTrigger from 'react-bootstrap/OverlayTrigger';
import Tooltip from 'react-bootstrap/Tooltip';

// board cover
import cover from '../../../../../assets/tooltips/secondary-tooltip-on-top.svg';

export default createBoard({
    name: 'Secondary tooltip on top',

    Board: () => {
        return (
            <OverlayTrigger
                placement="top"
                overlay={<Tooltip id="tooltip-top">Tooltip on bottom.</Tooltip>}
            >
                <Button variant="secondary">Tooltip on bottom</Button>
            </OverlayTrigger>
        );
    },
    tags: ['tooltip', 'overlay', 'top', 'secondary'],
    cover,
});
