import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
import OverlayTrigger from 'react-bootstrap/OverlayTrigger';
import Popover from 'react-bootstrap/Popover';
// board cover
import cover from '../../../../../assets/popovers/secondary-popover-on-top.svg';

export default createBoard({
    name: 'Secondary popover on top',

    Board: () => {
        return (
            <OverlayTrigger
                trigger="click"
                placement="top"
                overlay={
                    <Popover id="popover-top">
                        <Popover.Header as="h3">Popover top</Popover.Header>
                        <Popover.Body>
                            And here's some amazing content.
                        </Popover.Body>
                    </Popover>
                }
            >
                <Button variant="secondary">Popover on top</Button>
            </OverlayTrigger>
        );
    },
    tags: ['popover', 'overlay', 'top', 'secondary'],
    cover,
});
