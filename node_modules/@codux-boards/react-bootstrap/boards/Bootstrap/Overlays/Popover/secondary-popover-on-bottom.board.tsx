import { createBoard } from '@wixc3/react-board';
import Button from 'react-bootstrap/Button';
import OverlayTrigger from 'react-bootstrap/OverlayTrigger';
import Popover from 'react-bootstrap/Popover';
// board cover
import cover from '../../../../../assets/popovers/secondary-popover-on-bottom.svg';

export default createBoard({
    name: 'Secondary popover on bottom',

    Board: () => {
        return (
            <OverlayTrigger
                trigger="click"
                placement="bottom"
                overlay={
                    <Popover id="popover-bottom">
                        <Popover.Header as="h3">Popover bottom</Popover.Header>
                        <Popover.Body>
                            And here's some amazing content.
                        </Popover.Body>
                    </Popover>
                }
            >
                <Button variant="secondary">Popover on bottom</Button>
            </OverlayTrigger>
        );
    },
    tags: ['popover', 'overlay', 'bottom', 'secondary'],
    cover,
});
