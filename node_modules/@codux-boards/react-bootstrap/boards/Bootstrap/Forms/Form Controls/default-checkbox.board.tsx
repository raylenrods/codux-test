import { createBoard } from '@wixc3/react-board';
import Form from 'react-bootstrap/Form';

// board cover
import cover from './default-checkbox.svg';

export default createBoard({
    name: 'Default Checkbox',
    Board: () => {
        return <Form.Check type="checkbox" id="check-1" label="Option 1" />;
    },
    tags: ['input', 'form', 'checkbox', 'control'],
    cover,
});
