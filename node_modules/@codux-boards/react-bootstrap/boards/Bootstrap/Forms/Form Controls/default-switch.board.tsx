import { createBoard } from '@wixc3/react-board';
import Form from 'react-bootstrap/Form';

// board cover
import cover from './default-switch.svg';

export default createBoard({
    name: 'Switch',
    Board: () => {
        return (
            <Form.Check type="switch" id="switch" label="Check this switch" />
        );
    },
    tags: ['input', 'form', 'switch', 'control'],
    cover,
});
