import { createBoard } from '@wixc3/react-board';
import Form from 'react-bootstrap/Form';

// board cover
import cover from './default-radio.svg';

export default createBoard({
    name: 'Default Radio',
    Board: () => {
        return <Form.Check type="radio" id="radio-1" label="Option 1" />;
    },
    tags: ['input', 'form', 'radio', 'control'],
    cover,
});
